apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-alerting-provisioning
  namespace: observability
  labels:
    grafana_alert: "1"
data:
  contactpoints.yaml: |
    apiVersion: 1
    contactPoints:
      - orgId: 1
        name: discord-channel
        receivers:
          - uid: discord-receiver
            type: discord
            disableResolveMessage: false
            settings:
              url: https://discord.com/api/webhooks/1499942128460107856/Ev-kyQjDXkWqSK1kvQz_5WmTS5xsi1kUzkxY5b0PaHBxWOcXAHGVtBPLwbTEhxZxwkXq
              title: |
                {{ if eq .Status "firing" }}ALERTA DISPARADO{{ else }}ALERTA RESOLVIDO{{ end }} - {{ .CommonLabels.alertname }}
              message: |
                **Status:** {{ .Status }}
                **Servico:** {{ .CommonLabels.service }}
                **Severidade:** {{ .CommonLabels.severity }}
                {{ range .Alerts }}
                **Resumo:** {{ .Annotations.summary }}
                **Descricao:** {{ .Annotations.description }}
                {{ end }}
              avatar_url: https://grafana.com/static/img/menu/grafana2.svg
              use_discord_username: false

      - orgId: 1
        name: pagerduty-incidents
        receivers:
          - uid: pagerduty-receiver
            type: pagerduty
            disableResolveMessage: false
            settings:
              integrationKey: 2816c9c1b4af470dd002b15c3398b989
              severity: critical
              class: solidarytech
              component: '{{ .CommonLabels.service }}'
              group: '{{ .CommonLabels.severity }}'
              summary: '{{ .CommonLabels.alertname }} - {{ .CommonLabels.service }}'

      - orgId: 1
        name: github-self-healing
        receivers:
          - uid: github-receiver
            type: webhook
            disableResolveMessage: true
            settings:
              url: https://api.github.com/repos/$GITHUB_REPO_FULL_NAME/dispatches
              httpMethod: POST
              authorization_scheme: Bearer
              authorization_credentials: $GITHUB_DISPATCH_TOKEN
              maxAlerts: 1
              payload:
                  template: |
                    {
                      "event_type": "rollout-restart",
                      "client_payload": {
                        "service": "{{ if .Alerts }}{{ (index .Alerts 0).Labels.service }}{{ end }}",
                        "alertname": "{{ if .Alerts }}{{ (index .Alerts 0).Labels.alertname }}{{ end }}",
                        "fired_at": "{{ if .Alerts }}{{ (index .Alerts 0).StartsAt }}{{ end }}"
                      }
                    }
                  vars: {}

  policies.yaml: |
    apiVersion: 1
    policies:
      - orgId: 1
        receiver: discord-channel
        group_by: ["alertname", "service"]
        group_wait: 10s
        group_interval: 1m
        repeat_interval: 30m
        routes:
          - receiver: discord-channel
            object_matchers:
              - ["team", "=", "solidarytech"]
            group_wait: 10s
            group_interval: 1m
            repeat_interval: 30m
            continue: true
          - receiver: pagerduty-incidents
            object_matchers:
              - ["severity", "=", "critical"]
            group_wait: 10s
            group_interval: 1m
            repeat_interval: 30m
            continue: true
          - receiver: github-self-healing
            object_matchers:
              - ["action", "=", "rollout-restart"]
            group_wait: 10s
            group_interval: 5m
            repeat_interval: 1h
            continue: true

  rules.yaml: |
    apiVersion: 1
    groups:
      - orgId: 1
        name: solidarytech-critical
        folder: SolidaryTech
        interval: 30s
        rules:
          - uid: pod-crashloop
            title: PodCrashLoopBackoff
            condition: B
            data:
              - refId: A
                relativeTimeRange:
                  from: 300
                  to: 0
                datasourceUid: prometheus
                model:
                  datasource:
                    type: prometheus
                    uid: prometheus
                  editorMode: code
                  expr: 'sum by (namespace, pod) (kube_pod_container_status_waiting_reason{reason="CrashLoopBackOff",namespace=~"donation-service"})'
                  instant: true
                  range: false
                  intervalMs: 1000
                  maxDataPoints: 43200
                  refId: A
              - refId: B
                relativeTimeRange:
                  from: 0
                  to: 0
                datasourceUid: __expr__
                model:
                  conditions:
                    - evaluator:
                        params: [0]
                        type: gt
                      operator:
                        type: and
                      query:
                        params: [A]
                      reducer:
                        type: last
                      type: query
                  datasource:
                    type: __expr__
                    uid: __expr__
                  expression: A
                  intervalMs: 1000
                  maxDataPoints: 43200
                  refId: B
                  type: threshold
            noDataState: OK
            execErrState: Error
            for: 2m
            isPaused: false
            annotations:
              summary: "Pod no namespace donation-service em CrashLoopBackOff"
              description: "Pelo menos um pod de microsservico donation-service esta em CrashLoopBackOff ha mais de 2 minutos."
            labels:
              severity: critical
              service: donation-service
              action: rollout-restart
              team: solidarytech

          - uid: slo-fast-burn-donation
            title: SLOFastBurnDonation
            condition: B
            data:
              - refId: A
                relativeTimeRange:
                  from: 3600
                  to: 0
                datasourceUid: prometheus
                model:
                  datasource:
                    type: prometheus
                    uid: prometheus
                  editorMode: code
                  expr: '((sum(increase(http_server_duration_milliseconds_count{service_name="donation-service",http_status_code=~"5.."}[1h])) or vector(0)) / sum(increase(http_server_duration_milliseconds_count{service_name="donation-service"}[1h]))) / 0.005'
                  instant: true
                  range: false
                  intervalMs: 1000
                  maxDataPoints: 43200
                  refId: A
              - refId: B
                relativeTimeRange:
                  from: 0
                  to: 0
                datasourceUid: __expr__
                model:
                  conditions:
                    - evaluator:
                        params: [14.4]
                        type: gt
                      operator:
                        type: and
                      query:
                        params: [A]
                      reducer:
                        type: last
                      type: query
                  datasource:
                    type: __expr__
                    uid: __expr__
                  expression: A
                  intervalMs: 1000
                  maxDataPoints: 43200
                  refId: B
                  type: threshold
            noDataState: OK
            execErrState: Error
            for: 5m
            isPaused: false
            annotations:
              summary: "Error budget do donation-service queimando rapido"
              description: "Burn rate acima de 14.4x na ultima hora. Nesse ritmo o error budget mensal de 0.5% acaba em cerca de 2 dias. Verifique a taxa de erros 5xx no dashboard SRE."
            labels:
              severity: critical
              service: donation-service
              team: solidarytech