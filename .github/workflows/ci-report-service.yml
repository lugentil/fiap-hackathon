name: CI - report-service

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ["report-service/**"]
  pull_request:
    branches: [main]
    paths: ["report-service/**"]

permissions:
  contents: write

jobs:
  ci:
    uses: ./.github/workflows/_ci-python.yml
    with:
      service_name: report-service
      service_path: report-service
      ecr_repository: solidarytech/report-service
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
